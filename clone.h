#ifndef CLONE
#define CLONE

// Function to clone a vector of size n
// It receives to parameters: the int vector arr[] and the int size 
// It returns a new int vector of size n with all the elements of the original vector
void* cloneVector(int arr[], int size);

#endif